node_modules
**/node_modules

.git
.github
.turbo
dist

# Next.js build output — the image builds its own; copying a host build in
# would ship stale pages and break the standalone trace.
**/.next

.env
.env.*
!.env.example

**/src/generated

.DS_Store
Makefile
docker-compose.yml
README.md
